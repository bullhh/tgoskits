# Build-time config for StarryOS on Orange Pi 5 Plus via U-Boot.
# U-Boot serial/DTB settings live in the separate U-Boot runner config.
env = {}
log = "Info"
features = ["orangepi-5-plus"]
max_cpu_num = 8
plat_dyn = true
