# Build-time config template for StarryOS on Orange Pi 5 Plus.
# Runtime U-Boot settings live in `orangepi-5-plus-uboot.toml`.
env = {}
log = "Info"
features = ["orangepi-5-plus"]
max_cpu_num = 8
plat_dyn = true
